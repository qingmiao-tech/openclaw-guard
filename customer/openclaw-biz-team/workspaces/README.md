# 业务协作团队工作目录

本目录对应 `openclaw.json` 中 7 个 Agent 的独立工作空间。

- 每个子目录下 `SOUL.md` 定义角色身份与边界。
- 每个子目录下 `USER.md` 定义客户画像和沟通偏好（建议客户上线前补全）。
- 每个子目录下 `AGENTS.md` 定义执行协议、输出标准和协作方式。
- 默认总控 Agent 为 `task-hub`，负责拆解任务、并行委派、结果汇总。
- 如需沉淀长期资料，可在对应子目录新增 `knowledge/`、`templates/`、`SOP/`。