{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the OpenClaw Guard shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
